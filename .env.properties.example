# Local MySQL credentials
DB_USERNAME=root
DB_PASSWORD=replace_with_local_mysql_password

# Optional bootstrap password for the initial administrator account
# AUCTION_ADMIN_PASSWORD=replace_with_bootstrap_admin_password
